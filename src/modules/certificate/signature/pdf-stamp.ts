// src/modules/certificate/signature/pdf-stamp.ts
//
// OBSOLETO. A assinatura deixou de ser um "carimbo" sobreposto ao PDF.
// Agora ela é renderizada dentro do próprio template do certificado
// (ver `certificate-signature.service.ts` + templates em `../pdf`), o que
// garante o bloco centralizado com logo + nome + data + QR.
//
// Arquivo mantido vazio apenas por compatibilidade — pode ser removido.
export {};
